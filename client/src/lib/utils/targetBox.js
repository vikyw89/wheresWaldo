// Create a target box following user pointer location when pointer is inside the image...

const mouseCoord = (event) => {
    const XCoord = event.clientX
    const YCoord = event.clientY
    return {X:XCoord, Y:YCoord}
}

const frame = () => {
    
}